{{- /* stub — populated in v0.9.1+. v0.9.0 only ships the observer template. */ -}}
This prompt is a placeholder shipped with bough v0.9.0. The body
will land in v0.9.1 (= evolve / GATE 5 judge / cluster label) or
v0.9.2 (= SessionEnd evolve-claudemd handler).
