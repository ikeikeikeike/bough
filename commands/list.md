---
description: List the bough worktrees registered in .bough-ports.json.
allowed-tools: Bash(bough:*)
---

Run `bough list` and present the registered worktrees to the user (name, branch,
and allocated ports). If none are registered, say so plainly.
